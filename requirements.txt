# Core Selene dependencies (Fedora reference platform and native Windows).
# Optional integrations fail gracefully when absent; see README and AGENTS.md.

# Vector database for the searchable vault / codebase index
chromadb>=0.3.25
# DBus support for native Linux desktop integrations (Spotify MPRIS).
# Linux-only: must never install or build on native Windows.
dbus-python; sys_platform == "linux"
# DuckDuckGo search library for web querying
ddgs
# Official Ollama Python client for LLM interaction
ollama>=0.4.0
# PDF text extraction (always useful). pdf2image needs system Poppler for
# page-to-image conversion only; missing Poppler does not block startup.
pdf2image
pypdf>=5.1.0
python-docx>=1.2.0
# Styled, paginated PDF note/reference generation (optional tool capability).
reportlab>=4.0.0
# Excel spreadsheet reading and writing (.xlsx and legacy .xls)
openpyxl>=3.1.0
xlrd>=2.0.1
xlwt>=1.3.0
# Standard HTTP requests library
requests>=2.28.0
# Google Calendar/Tasks OAuth and encrypted credential storage.
# Missing Google packages disable only the google_workspace tool.
cryptography>=42.0.0
google-api-python-client>=2.100.0
google-auth-oauthlib>=1.2.0
keyring>=24.0.0
# Terminal UI styling and Markdown rendering
rich>=13.0.0
# Full-screen CLI TUI (Claude Code / Grok Build style). Optional at import
# time — classic scrollback mode works without it; --cli prefers Textual.
textual>=1.0.0
# HTML parsing for web_scrape (optional; tool reports a capability error if absent)
beautifulsoup4>=4.12.0
